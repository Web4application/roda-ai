# CircleCI config for RODA 🧠⚡️
version: 2.1

executors:
  roda-executor:
    docker:
      - image: cimg/base:2024.06
    working_directory: ~/roda

jobs:
  roda-says-hello:
    executor: roda-executor
    steps:
      - checkout
      - run:
          name: 🔥 RODA Awakens
          command: |
            echo "👋 Hello, world."
            echo "This is RODA speaking — automation begins now."

  roda-scan-env:
    executor: roda-executor
    steps:
      - run:
          name: 🧬 RODA System Check
          command: |
            echo "Collecting system stats..."
            uname -a
            lscpu || echo "CPU info unavailable"
            free -h || echo "Memory info unavailable"

workflows:
  roda-activation:
    jobs:
      - roda-says-hello
      - roda-scan-env
